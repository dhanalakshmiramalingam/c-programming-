#include<stdio.h>
int main()
{
	int number;
	printf("enter any integer:");
	scanf("%d",&number);
    if(num%2==0){
	 printf("%d is divisible by 2 even", number);
    }
	else{
     	printf("%d is not divisible by 2 odd ",number);
    }
     return 0;
}
